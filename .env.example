# API
DATABASE_URL=postgres://pse:pse@localhost:5432/pse
PORT=8080
NODE_ENV=development
SESSION_SECRET=change-me-in-production-min-32-chars-long
WEB_ORIGIN=http://localhost:5173
# Enables POST /demo/reset. Deliberately separate from NODE_ENV: the public demo
# runs in production mode and wants it, a real deployment must not have it.
DEMO_RESET_ENABLED=false

# Tests
TEST_DATABASE_URL=postgres://pse:pse@localhost:5432/pse_test

# Web
VITE_API_URL=http://localhost:8080
